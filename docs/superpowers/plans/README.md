# docs/superpowers/plans/ — plany implementacji

Plany realizacji rozbite na fazy/zadania (checkboxy `- [ ]`).

| Plik | Rola |
|---|---|
| `2026-05-23-kalisthenos-v1-phase-1-foundation.md` | Faza 1 (fundament): RR7 + TypeScript + Postgres/Drizzle, auth sesyjna, flow zaproszeń, szkielety layoutów trenera/podopiecznego. Zawiera cel, architekturę, stack i kroki. |
| `2026-05-31-ai-dev-process-tooling.md` | Wdrożenie procesu AI-developmentu: skill `kalisthenos-dev-flow` + commandy `/feature` `/fix`, `.claude/settings.json`, edycja `.gitignore`/`CLAUDE.md`/`README.md` i pamięci (zwrot ku TDD + review-per-task). |
| `2026-05-31-toggle-rpe-cwiczenia.md` | Przełącznik zbierania RPE (trudności 1–10) per ćwiczenie: schemat (`tracks_rpe` + nullowalny `difficulty`), warstwa zapisu (logowanie/biblioteka), reguła odczytu `avgRpe: number \| null` w statystykach/progresji/wrapped, UI detalu/wykresów, testy. 11 zadań + bramki. |
| `2026-06-01-konsultacje-harmonogram-faza1.md` | Konsultacje — Faza 1 (rdzeń natywny): przebudowa `consultations` w byt o cyklu życia, `consultation_schedules`, generator dat (`consultation-recurrence.ts`), CRUD harmonogramu + materializacja, statusy/przejścia, UI trenera i kalendarz podopiecznego, ręczny `meeting_url`. **Bez Google.** |
| `2026-07-22-wideo-rozdzielony-upload.md` | Rozdzielenie uploadu nagrań serii od zapisu sesji (plasterek P2 z [`../../audyt.md`](../../audyt.md)): override klucza w rate-limicie + kubełek `upload`, trasa zasobowa `upload/wideo`, walidacja identyfikatorów przy zapisie (`uploaded_by` jako rdzeń bezpieczeństwa), sweeper sierot bez migracji, komponent `VideoUploadField` z postępem XHR, szkic v3. 6 zadań + `/security-review`. |
| `2026-07-26-pomysly-zgloszenia.md` | Zgłoszenia podopiecznych („Pomysły"): tabela `feature_requests` + enumy, czysty moduł `feature-request-types.ts` (Zod, etykiety PL, `statusPresentation`, `canTraineeDelete`), repo `feature-requests.ts` z tenant-scope, trasa podopiecznego z formularzem, skrzynka trenera + szczegół z odpowiedzią, odznaka „nowe" w sidenavie, itest tenant-scope. 7 zadań + `/security-review`. |
| `2026-07-26-formularz-startowy.md` | Formularz startowy (onboarding podopiecznego): tabele `onboarding_forms` + `onboarding_form_items` ze snapshotem jednostki, czysty moduł `onboarding-form-types.ts` (Zod, `answerLabel`, parser równoległych pól), repo `onboarding-forms.ts`, picker w modalu zaproszenia (zaproszenie + formularz w jednej transakcji), ekran podopiecznego poza layoutem, bramka za bramką płatności (`stripe/gate.ts`), widok wyników trenera. 8 zadań + `/security-review`. |
| `2026-07-28-faza-a-uszczelnienie-szwu.md` | **Faza A rozbicia FE/BE** (spec: [`../specs/2026-07-28-rozbicie-fe-be-analiza-ddd-design.md`](../specs/2026-07-28-rozbicie-fe-be-analiza-ddd-design.md)): usunięcie z tras 43 zapytań budowanych inline i 3 transakcji — wszystko przez fasady `app/lib/*`, zero zmian zachowania. Wydobycie repozytoriów podopiecznych/ćwiczeń/planów, pulpitu trenera, tożsamości, plików i dedupu webhooka; `findTraineeOfTrainer` przeniesione z `progression.ts` do `trainees.ts` (6 duplikatów); jedno świadome utwardzenie (`trainer_id` w `WHERE` przy archiwizacji/edycji ćwiczenia); test strażnik `app/routes/no-direct-db.test.ts`. 10 zadań + `/security-review`. |
| `2026-09-01-uwierzytelnianie-na-tokenach-be.md` | **Etap 2 krok 2 integracji FE/BE** (spec: [`../specs/2026-09-01-uwierzytelnianie-na-tokenach-be-design.md`](../specs/2026-09-01-uwierzytelnianie-na-tokenach-be-design.md)): warstwa klienta dostaje stronę ZAPISUJĄCĄ. `ApiError` niesie `Retry-After` z nagłówka, nowy `api/auth-session.ts` (`startSession`, `acceptInvite`, `endSession`, `AuthError`) woła kontrakt, trzy trasy (`login`, `zaproszenie/$token`, `wyloguj`) przechodzą na niego, a stara sesja bazodanowa znika z drzewa razem z `session.ts`, `password.ts`, `cookie.ts`, pruningiem w `root.tsx` i kolizją dwóch `clearSessionCookie`. Limit prób i stały czas logowania usunięte z FE, **bo zmierzone w repozytorium BE**: throttler kluczowany e-mailem i pełny hasz dla nieistniejącego konta są już po tamtej stronie. 7 zadań, w tym osobne na bramkę „nic w `app/` nie dotyka `__Host-kth_session`". |
| `2026-08-31-warstwa-klienta-api-fe.md` | **Etap 2 krok 1 integracji FE/BE** (spec: [`../specs/2026-08-31-warstwa-klienta-api-fe-design.md`](../specs/2026-08-31-warstwa-klienta-api-fe-design.md)): `db: Db` zastąpione przez `api: Api` na tej samej pozycji w sygnaturze. Pakiet `@kalisthenos/api-client` z GitHub Packages i dwa adresy bazowe, fabryka klienta z `throwOnError` i mapowaniem koperty na `ApiError`, `orNull` jako narzędzie reguły „`\| null` łapie 404", `refreshOnce` z oknem łaski i mapą w locie (BE przy ponownym użyciu tokenu gasi cały łańcuch sesji), middleware `v8_middleware` ładujące klienta i użytkownika raz na żądanie, `requireUser` synchroniczne z `context` z rolą jako listą, przepięcie 77 wywołań w 51 trasach, `categories.ts` jako dowód wzorca. 8 zadań, w tym osobne na test „pięć loaderów, jedna rotacja". |
| `2026-09-01-cwiczenia-na-kontrakcie.md` | **Etap 2 krok 3 integracji FE/BE, pierwszy obszar** (spec: [`../specs/2026-08-29-integracja-fe-be-design.md`](../specs/2026-08-29-integracja-fe-be-design.md)): `app/lib/exercises.ts` przechodzi z Drizzle na kontrakt — siedem funkcji bierze `api: Api` bez `trainerId` (zakres niesie token), stronicowanie i przycinanie strony przechodzą do BE (`countExercisesForTrainer` znika, bo `total` przychodzi z listą), `ExerciseWithDemo` ustępuje podpisanemu `demoUrl`, a bramka „wariant aktywnej umiejętności" przy archiwizacji przenosi się do BE (`409 EXERCISE_IS_SKILL_VARIATION`), zabierając ostatnią zależność obszaru od `skills.ts`. `uploadExerciseDemo` wprowadza dwufazową wysyłkę na kontrakcie; zapis z demo rozpada się z jednej transakcji na sekwencję wysyłka → utworzenie → podpięcie. Znika `tests/exercises-repo.itest.ts` (spec §10), zastąpiony testami modułu przeciw podstawionemu klientowi. 8 zadań; przegląd końcowy wyłapał, że podpisany odnośnik z BE jest ŚCIEŻKĄ, nie adresem — stąd `publicFileUrl` i `API_PUBLIC_URL` w warstwie klienta. |
| `2026-09-02-plany-na-kontrakcie.md` | **Etap 2 krok 3 integracji FE/BE, drugi obszar** (spec: [`../specs/2026-08-29-integracja-fe-be-design.md`](../specs/2026-08-29-integracja-fe-be-design.md)): `app/lib/plans.ts` przechodzi z Drizzle na kontrakt — lista oddaje całą stronę razem z `counts` (trzy zapytania → jedno), sortowania bez słownika (identyczne z kontraktem), szkic pary czyta się z `draftId` szczegółu i z `409 PLAN_DRAFT_EXISTS` (`{ id, created }` zamiast pre-checków), zapis przez jawne `toSavePlanDto` (zdejmuje `id`, normalizuje tempo per rodzaj bloku — BE odrzuca pola spoza DTO), usuwanie bez `logCount`. Nowy `app/lib/views.ts` (nawigacja i pulpit z widoków przekrojowych BE, jedno wywołanie na ekran) **wycofuje decyzję A5** ćwiczeń; licznik planów przestaje liczyć zarchiwizowane (BE nadrzędne). Znika `tests/plans-repo.itest.ts`. 7 zadań. |
| `2026-09-02-dziennik-na-kontrakcie.md` | **Etap 2 krok 3 integracji FE/BE, trzeci obszar** (spec: [`../specs/2026-08-29-integracja-fe-be-design.md`](../specs/2026-08-29-integracja-fe-be-design.md)): `app/lib/workouts.ts` przechodzi z Drizzle na kontrakt — kontrakt rozdziela trasy podopiecznego i trenera, więc `loadLogForViewer` staje się parą `loadMyLog`/`loadTraineeLog`, listy oddają całą stronę (liczniki znikają), sesję do logowania bierze się po id (`findActivePlanForTrainee` znika, o dopuszczalności decyduje BE), a zapis to jedno `POST /v1/workout-logs` z kluczem idempotencji i rekordami w odpowiedzi (`assertOwnedUnclaimedVideos`, `detectNewPRsForLog` znikają — BE). Pulpit podopiecznego w całości z `GET /v1/me/home` (osiem zapytań trzech modułów → jeden widok; `getHeroStats`/`getThisWeekStats`/`getEffortBalance` znikają), pulpit trenera domyka B5. `listClientsForTrainer` przechodzi do `trainees.ts` (`GET /v1/trainees`). Nagranie serii wysyłane dwufazowo przez kontrakt (`uploadSetVideo`); `/upload/wideo` zostaje trasą zasobową dla XHR, a bramka formularza i limit wysyłek przechodzą do BE (`rate-limit.ts` znika). Znikają cztery `tests/*.itest.ts`. Dziesięć widocznych ubytków spisanych jako luki L1–L10 (L1–L8 to pola do dołożenia addytywnie po stronie BE, L9–L10 to decyzje po stronie FE). 9 zadań. |
| `2026-09-03-reszta-app-lib-na-kontrakcie.md` | **Etap 2 krok 3 integracji FE/BE, cała reszta w trzech falach** (spec: [`../specs/2026-08-29-integracja-fe-be-design.md`](../specs/2026-08-29-integracja-fe-be-design.md)): sześć segmentów ciętych po sprzężeniach — fala 1: umiejętności i rozwój (S1), zgłoszenia i formularz startowy (S2), konsultacje i kalendarz (S3) równolegle; fala 2: pliki i sylwetka (S4), potem podopieczni, statystyki i wrapped (S5); fala 3: sprzątanie Stripe'a, bazy i bramki (S6). Segmenty jednej fali wykonują osobni agenci **w trybie lekkim** (bez typecheck/lint/vitest/build — jeden przebieg vitest na segment robi koordynator), z jawną własnością plików i regułą „w plikach wspólnych tylko własne linie, tylko Edit". Briefy per segment z mapowaniem funkcja → SDK, rejestr luk z **LK1**: przepływu „Połącz z Google" nie da się przepiąć przy FE wołającym BE serwer-do-serwera (ciastko nonce'a na hoście BE), więc `google/connections.ts`, `oauth.ts`, `crypto.ts` i trasy `integracje.google*` zostają na Drizzle do decyzji poza planem. |
| `2026-06-02-konsultacje-google-faza2.md` | Konsultacje — Faza 2 (Google Calendar/Meet): tabela `google_calendar_connections`, `app/lib/google/*` (crypto AES-256-GCM, OAuth, klient Calendar, repo połączeń, orkiestracja sync), trasy OAuth połącz/rozłącz, sync wychodzący best-effort wpięty w mutacje, nowe env. 10 zadań + `/security-review`. |

---
Konwencja i zasady aktualizacji dokumentacji: [`../../../CLAUDE.md`](../../../CLAUDE.md).
