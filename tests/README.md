# tests/ — testy integracyjne

Testy integracyjne `*.itest.ts` uruchamiane na realnym PostgreSQL przez
**testcontainers** (Docker). Pokrywają krytyczne przepływy, których nie da się
sprawdzić bez bazy (tenant-scope, zapisy transakcyjne, kaskady, autoryzacja).

- Uruchamia je właściciel pod Dockerem: `npm run test:itest`
  (vitest z dłuższym timeoutem; filtr ścieżki `tests`).
- Testy jednostkowe (bez DB) leżą przy kodzie jako `app/**/*.test.ts` i lecą
  przez `npm run test:unit` — `tests/` ich nie zawiera.

| Plik | Zakres |
|---|---|
| `consultations.itest.ts` | Repo konsultacji (`app/lib/consultations.ts`): tworzenie z punktami, izolacja tenantów (obcy trener/podopieczny → brak dostępu), guard własności przy zmianie statusu punktu, podmiana punktów przy edycji, kaskadowe usuwanie, liczniki. |
| `google-sync.itest.ts` | Synchronizacja Google Calendar (`app/lib/google/sync.ts`) na realnej bazie, z zamockowanym `@googleapis/calendar`: `syncUpsertOne` (insert z `conferenceDataVersion:1`/`sendUpdates:"all"`/uczestnikiem, persystencja `google_event_id`+`meetingUrl`; błąd Google nie rzuca i nie rusza stanu natywnego), repo-helpery `listGoogleEventIdsForPair`/`listCancelledGoogleEventIds`, `syncCancelAllForPair`, `syncCancelStaleSchedule`, oraz **`syncBackfillPair`** — wstawia brakujące, `patch`-uje istniejące (regresja stref: `dateTime` bez „Z" + `timeZone: Europe/Warsaw`; `timesOnly`, więc bez `summary`/`description`), pomija przeszłe i odwołane, nie patchuje właśnie wstawionych, odtwarza zdarzenie skasowane w Google (patch → 404), błąd 500 jednego terminu nie przerywa reszty (`{attempted:2, synced:1}`), `connected:false` bez połączenia. Każdy przypadek ma własną parę trener-podopieczny (`makePair`), bo sync liczy wszystkie terminy pary. |
| `progression-tenant-scope.itest.ts` | Tenant-scope Progresji (`app/lib/progression.ts`): `findTraineeOfTrainer` (mieszka w `app/lib/trainees.ts`, tu tylko re-eksportowana dla starych importerów; obcy trener → null, właściwy → podopieczny), `listProgressionExercises` (lista P_A bez logów innego podopiecznego), `getExerciseProgression` (dane tylko z logów P_A, cudze ćwiczenie → null), `getProgressionComparison` (serie + pominięte). |
| `trainees-repo.itest.ts` | Repozytorium podopiecznych (`app/lib/trainees.ts`): `getTraineeOfTrainer` (pełny wiersz tylko własnemu trenerowi, obcy → null), `listTraineesOfTrainer` (pomija zarchiwizowanych, sortuje po nazwie), `countTraineesOfTrainer` (celowo liczy również zarchiwizowanych — utrwala różnicę względem `listTraineesOfTrainer`, żeby nikt jej nie „ujednolicił"), tenant-scope obu list/liczników. |
| `plans-repo.itest.ts` | Repozytorium planów (`app/lib/plans.ts`): `countPlansByStatusForTrainer` pomija zarchiwizowane; `listPlansForTrainer`/`countPlansForTrainer` — szukajka po nazwie planu ALBO nazwie podopiecznego (ten sam zestaw warunków w obu, `innerJoin` na `users`), `sessionCount` z CTE `LEFT JOIN`+`COALESCE` (plan bez sesji zostaje na liście z `sessionCount === 0`, nie znika), tenant-scope; `findPlanStatusForTrainer` → `null` dla obcego trenera; `countPlansForTrainerByStatus(…, null)` liczy WSZYSTKIE plany (także zarchiwizowane) w kontraście do `countPlansByStatusForTrainer`; `listPlansForTrainee` zwraca też zarchiwizowane (widok klienta) — utrwala trzy różne, celowo nieujednolicone zakresy archiwizacji; `countSessionsInPlan` zwraca 0 dla planu bez sesji. Rozszerzony też o pulpit trenera (`app/lib/workouts.ts`): `listRecentLogsForTrainer` zwraca logi wszystkich podopiecznych trenera, najnowsze pierwsze; `countLogsForTrainerSince` liczy tylko od podanej daty i tylko własne logi trenera. |
| `exercises-repo.itest.ts` | Repozytorium ćwiczeń (`app/lib/exercises.ts`): `listActiveExercisesForTrainer` (aktywne po nazwie, NIE odfiltrowuje wariantów umiejętności — regresja pilnująca różnicy względem `listAssignableExercises` ze `skills.ts`, która okroiłaby picker edytora planów), `countActiveExercisesForTrainer`, `getExerciseWithDemoForTrainer`/`getExerciseForTrainer` (tenant-scope, brak przecieku), `setExerciseArchived` (no-op dla obcego trenera dzięki `trainer_id` w `WHERE`; archiwizacja/przywrócenie własnego ćwiczenia); `listExercisesForTrainer`/`countExercisesForTrainer` — filtr po szukajce (`q`), tagu (`tag`) i jednostce (`unit`), zgodność liczby z listą, tenant-scope (obcy trener nie widzi cudzych ćwiczeń w liczniku). |
| `lists-sort-filter-tenant-scope.itest.ts` | Tenant-scope + poprawność sort/filtr/szukajki dla list: `listLogsForTrainee` (sort=hardest/easiest/sets_desc, video=with/without, q, izolacja traineeB), `countLogsForTrainee` (video+q), `listClientsForTrainer` (sort=most_sessions + paginacja, q, plan=with/without, izolacja trenerów), `listConsultationsForTrainee` (open=with_open, q, sort=most_open/date_asc, izolacja traineeB). |
| `rpe-toggle.itest.ts` | Przełącznik RPE per ćwiczenie (`exercises.tracksRpe`): zapis `difficulty = NULL` dla ćwiczenia bez RPE, sesja mieszana liczy `avgDifficulty` tylko z ocenionych serii, `getExerciseProgression` zwraca `avgRpeInRange = null` gdy brak ocen, `getEffortBalance` pomija sesje bez RPE (nie rzuca i zwraca liczbę). |
| `skills.itest.ts` | Umiejętności/awanse (`app/lib/skills.ts`, `app/lib/skill-progression.ts`): tenant-scope (obcy trener → null/`SkillError`), pełny cykl create→addVariation×3→reorder→setStartingLevel→awans→regres + `getSkillMapForTrainee` (poprawny `currentVariationId` = najnowsze zdarzenie, historia), `UNIQUE(exercise_id)` („exercise taken"), `ON DELETE RESTRICT` przy `removeVariation` („referenced"), guardy `recordAdvancement` („no start"/„same level"). |
| `skill-tier.itest.ts` | Tier umiejętności (`app/lib/skills.ts`): zapis/odczyt tieru, `DEFAULT 'basic'` z migracji, tenant-scope (obcy trener nie odczyta ani nie nadpisze), twarda walidacja „prereq nie z wyższego tieru" w `addPrerequisite` (równy i niższy przechodzą), filtr pickera `listAssignablePrerequisites`, oraz kolizja po zmianie tieru — `updateSkill` przechodzi, a `listConflictingPrerequisites` ją raportuje. |
| `onboarding-payment.itest.ts` | Onboarding płatności (OB-3): po rejestracji z zaproszenia z kwotą `setMonthlyAmount` tworzy wiersz `coaching_subscriptions` (status `none`, `amountGrosze`, `stripePriceId`) dla pary trener↔podopieczny; para bez wywołania → `getSubscriptionForPair` = null; tenant-scope (obcy trener → null). Klient Stripe mockowany. |
| `upload-wideo.itest.ts` | Trasa zasobowa `/upload/wideo` (realny Postgres + zapis na dysk w katalogu tymczasowym): `trainerId`/`uploadedBy`/`kind` brane z SESJI i stałej w kodzie, a pola `trainerId`/`kind` dorzucone do `FormData` są IGNOROWANE; trener odbity (trasa tylko dla podopiecznych); niezgodne magic-bytes → 400 i zero wierszy w `files`; brak pliku → 400; odpowiedź to czysty JSON parsowalny gołym `JSON.parse` (konsumuje ją XHR, nie React Router). |
| `workout-video-ids.itest.ts` | **Walidacja identyfikatorów nagrań przy zapisie** (`assertOwnedUnclaimedVideos`) — rdzeń bezpieczeństwa rozdzielonego uploadu. Kluczowy przypadek: nagranie INNEGO podopiecznego TEGO SAMEGO trenera musi zostać odrzucone (sam `trainer_id` tego nie wyłapie, chroni dopiero `uploaded_by`). Dalej: obcy tenant, zły rodzaj pliku, nagranie już podpięte, nieistniejące id, duplikat w jednym żądaniu, odrzucenie CAŁEGO zapisu gdy choć jedno id jest złe. |
| `orphan-sweeper.itest.ts` | Sweeper nagrań-sierot (`sweepOrphanSetVideos`): kasuje `set_video` po karencji i niepodpięte (wiersz + blob), NIE rusza podpiętych, świeższych niż karencja ani plików innego rodzaju. `deleteFileBlob` mockowany — testujemy wybór wierszy, nie dysk. |
| `feature-requests.itest.ts` | Zgłoszenia podopiecznych (`app/lib/feature-requests.ts`): nowe zgłoszenie dostaje status `new` i trafia do trenera autora; tenant-scope (trener B nie odczyta ani nie odpowie na zgłoszenie trenera A, cudzy podopieczny nie usunie); `respondToFeatureRequest` ustawia status+odpowiedź+`responded_at`, a sama zmiana statusu (odpowiedź `null`) `responded_at` NIE stempluje; autor usuwa własne zgłoszenie tylko dopóki ma status `new`; `countNewForTrainer`/`countForTrainee` liczą wyłącznie własne. |
| `onboarding-forms.itest.ts` | Formularz startowy (`app/lib/onboarding-forms.ts`) doczepiony do zaproszenia: pełny przepływ — formularz przypięty przy `consumeInvite` blokuje aplikację do wypełnienia (`hasPendingOnboarding`/`getPendingFormForTrainee`), wysyłka zapisuje odpowiedzi i zamyka formularz (`getFormForTrainer`); drugie wysłanie nie nadpisuje odpowiedzi i odbija się `OnboardingFormError`; niekompletny komplet odpowiedzi odrzucony; zaproszenie bez formularza zostawia flow bez zmian. Tenant-scope: `createOnboardingForm` odrzuca cudze ćwiczenie oraz ćwiczenie zarchiwizowane (osobny przypadek — inaczej usunięcie warunku `archived_at IS NULL` przeszłoby na zielono), podopieczny B nie zapisze odpowiedzi na pozycjach formularza podopiecznego A (formularz wybierany po `traineeId` z sesji), obcy trener nie widzi formularza (`getFormForTrainer`/`getFormStatusForTrainee` → `null`), usunięcie podopiecznego (`deleteTraineeFully`) kasuje formularz i pozycje kaskadowo. |
| `auth-repo.itest.ts` | Odczyty tożsamości (`app/lib/auth/users.ts`, `app/lib/auth/invite.ts`): `findUserByEmail` po dokładnym adresie (`null` gdy brak konta), `findInviteByToken` haszuje surowy token z URL-a i zwraca zaproszenie (`null` dla zmyślonego tokenu), `findDisplayName` zwraca nazwę wyświetlaną trenera (`null` dla nieistniejącego użytkownika). |
| `stripe-pause.itest.ts` | Pauza/wznowienie subskrypcji (RD-1): `pauseSubscription` ustawia status `paused` i woła Stripe `subscriptions.update` z `pause_collection.behavior === "void"`; `resumeSubscription` ustawia `active` i czyści `pause_collection` (pusta wartość); `applySubscriptionUpdate` z `paused:true` nadpisuje status na `paused`; tenant-scope (obcy trener → `SubscriptionError`). Klient Stripe mockowany. |
| `stripe-webhook.itest.ts` | Webhook Stripe (`app/lib/stripe/webhook.ts`) na realnym Postgresie: idempotencja księgi płatności (`applyChange(invoice)` dwa razy z tym samym `stripeInvoiceId` → jeden wiersz, upsert last-write-wins; invoice bez pary → pomijane), idempotencja statusu subskrypcji (`applyChange(subscription)` wielokrotnie → ostatni stan wygrywa, bez duplikatu wiersza; też end-to-end przez `mapEvent`), dedup po `event.id` (`processed_webhook_events`, insert-first/on-conflict-skip — odwzorowanie kontraktu trasy `routes/webhooks.stripe.tsx`) oraz bezpośrednio `claimWebhookEvent`/`releaseWebhookEvent`: pierwsze wystąpienie zwraca `true`, powtórka `false`, a po `releaseWebhookEvent` zdarzenie może być przetworzone ponownie (kontrakt retry po błędzie handlera). Klient Stripe mockowany (brak sieci — nie przechodzi przez `verifyAndParse`). |

Konwencja i zasady aktualizacji dokumentacji: [`../CLAUDE.md`](../CLAUDE.md).
